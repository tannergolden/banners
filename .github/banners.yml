# The banners THIS repository draws for itself: the kit eating its own
# cooking. The title, the tagline, every figure and the four link buttons
# under the footer are read from GitHub on each run of 🪧 Own Banners;
# only what is written here is fixed. Delete a line to hand it back to GitHub.
#
# Every key and its default: examples/banners.yml. The header is H2 Section,
# the default, over its F1 Title block, in blueprint, the default print, and
# the buttons are the defaults too: the first four of Issues, Pull Requests,
# Releases, Actions, Discussions and Contributors this repository has.
motto: Drafted, never fetched.
closing: Drawn at both ends. Fetched at neither.
